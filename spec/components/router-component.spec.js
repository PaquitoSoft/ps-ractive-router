var Ractive = require('ractive'),
	RouterComponent = require('../../src/components/router-component');

describe('Router component', function() {

	it('Should have its own template');

	it('Should have a router-handler dynamic component');

	it('Should observe componentName attribute so it re-renders when updated');

});