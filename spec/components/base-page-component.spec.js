var Ractive = require('ractive'),
	BasePage = require('../../src/components/base-page-component');

describe('Base page component', function() {

	beforeEach(function() {
		Ractive.components = [];
	});

	it('Should register page component globally');

	it('Should have its property name');

	it('Should register custom events');

	it('Should call provided callback when navigating');

	it('Should allow onconfig callback to be overloaded');
});